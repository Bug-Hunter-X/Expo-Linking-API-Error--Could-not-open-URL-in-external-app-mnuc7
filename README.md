This repository contains a bug report and solution for an uncommon error encountered when using Expo's Linking API to open URLs in external applications. The error, "could not open URL in external app", can stem from various issues, including incorrect URL formatting, missing target apps, or insufficient permissions.  The provided code demonstrates the problematic scenario and a robust solution for handling this error gracefully.